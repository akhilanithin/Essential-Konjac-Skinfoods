import React from 'react';
import ElementsList from '~/components/partials/elements/elements-list';

const ElementIndex: React.FC = () => {
    return <ElementsList />;
};

export default React.memo(ElementIndex);
